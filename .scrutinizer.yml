build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
  environment:
    php: 8.1.6

filter:
  excluded_paths:
    - examples/*
    - tests/*
    - vendor/*
    - .github/*
    - .phan/*
